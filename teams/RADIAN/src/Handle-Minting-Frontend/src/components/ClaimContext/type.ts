

export interface IClaimContext {
    handle: string,
    setHandle(val: string): void,
    step: number,
    setStep(val: number): void,
    passColor: string,
    setPassColor(val: string): void,
}

